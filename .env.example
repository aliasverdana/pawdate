# Vercel Postgres (or any Postgres)
# Example: postgres://USER:PASSWORD@HOST:5432/DB?sslmode=require
DATABASE_URL=""

# Base URL for generating magic links.
# In Vercel, you can set APP_URL to your production domain.
APP_URL="http://localhost:3000"

# Used to sign the session cookie (JWT). Generate with:
#   openssl rand -base64 32
AUTH_JWT_SECRET=""

# Email sending via Resend (recommended)
RESEND_API_KEY=""

# Resend FROM address. Use a domain you've verified in Resend.
# Example: Pawdate <hello@pawdate.app>
EMAIL_FROM=""
